{
  "name": "webar-zebra-poc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "copy-xr": "node scripts/copy-xr.mjs",
    "dev": "npm run copy-xr && vite --host",
    "build": "npm run copy-xr && tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "@8thwall/engine-binary": "^1.0.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
